import React from 'react';
import { Link } from 'react-router-dom';

const Error = () => (
  <div className='error-container'>
    <h1>Well, this can't be good</h1>
  </div>
);

export default Error;
